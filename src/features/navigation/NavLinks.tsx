import React from "react";
import NavItem from "./NavItem";

type Props = {};

const NavLinks = (props: Props) => {
  return (
    <div>
      <NavItem />
    </div>
  );
};

export default NavLinks;
